#pragma once
#include "HiberTopoBase.h"


class CTransitSub : public CHiberTopoBase
{
//public:
//	CTransitSub(Count_t transitCount,          //Transit层域的个数
//				Count_t stubCount,             //一个Transit相连的Stub的个数
//				Count_t lanCount,              //一个Stub相连的Lan的个数
//				vector<void*>&  TSdata,         //各个域内部的参数
//				IPAddr_t i= IPADDR_NONE,       //基IP
//				const Linkp2p& = Linkp2p::Default(),//连接的方式,下面如果不给出,给出各个域之间的联系方式,
//													//如域间没有给出如不给出,则继续用这种联系方式
//				SystemId_t = 0);               //分布式系统标识符 
//	/*
//	TSdata   的格式为：
//					 Transit的一些参数
//					 Tend
//					 Stub的一些参数
//					 Send
//					 Lan的一些参数
//					 Lend
//	*/
//	~CTransitSub(void);
//public:
//	bool GenerateTopo();
//private:
//	bool NewTransit();
//	bool NewStub();
//	bool NewLan();
//private:
//	vector<Transit*> transit;
//	vector<Stub*>    stub;
//	vector<Lan*>     lan;
};
